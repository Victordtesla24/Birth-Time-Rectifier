export const CONFIG = {
    // Animation settings
    ANIMATION_DURATION: 300,
    TRANSITION_DURATION: 500,
    FADE_DURATION: 200,
    
    // API settings
    API_BASE_URL: 'https://api.birthtimerectifier.com',
    API_ENDPOINT: 'https://api.birthtimerectifier.com',
    API_TIMEOUT: 10000,
    
    // Visualization settings
    CANVAS_WIDTH: 800,
    CANVAS_HEIGHT: 600,
    MAX_ZOOM: 5,
    MIN_ZOOM: 0.5,
    
    // Form settings
    MAX_EVENTS: 10,
    MIN_EVENTS: 1,
    
    // Validation settings
    DATE_FORMAT: 'YYYY-MM-DD',
    TIME_FORMAT: 'HH:mm',
    
    // Storage keys
    STORAGE_KEYS: {
        USER_DATA: 'btr_user_data',
        PREFERENCES: 'btr_preferences',
        RECENT_SEARCHES: 'btr_recent_searches'
    }
};
